apiVersion: v1
kind: Service
metadata:
  name: tops-frontend
  labels:
    app: tops-frontend
spec:
  type: LoadBalancer
  selector:
    app: tops-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
