apiVersion: v1
kind: Service
metadata:
  name: tuiv-frontend
  labels:
    app: tuiv-frontend
spec:
  type: ClusterIP
  selector:
    app: tuiv-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
