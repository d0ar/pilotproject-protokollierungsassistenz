apiVersion: v1
kind: Service
metadata:
  name: backend
  labels:
    app: tops-backend
spec:
  type: ClusterIP
  selector:
    app: tops-backend
  ports:
    - name: http
      port: 8010
      targetPort: 8010
